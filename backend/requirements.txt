# Core Vision SDK & Sponsor Tech
vision-agents[getstream,gemini,ultralytics,elevenlabs]==0.3.7

# Backend Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Computer Vision & Math (headless for server deployment)
opencv-python-headless>=4.8.0
numpy>=1.26.0

# Utils
python-dotenv>=1.0.1
pydantic>=2.9.0

# WebSockets Client
websockets>=12.0

# HTTP Client (Gemini REST fallback)
httpx>=0.27.0

# Chat Endpoint (OpenAI)
openai>=1.0.0

# PyTorch CPU-only (smaller footprint for deployment)
torch --index-url https://download.pytorch.org/whl/cpu
